// Allow side-effect imports of global stylesheets (e.g. `@/styles/globals.css`).
declare module '*.css';

// `zendro-bulk-create` is a plain JS package that ships no type declarations.
declare module 'zendro-bulk-create';
