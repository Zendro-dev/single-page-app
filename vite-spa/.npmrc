# zendro-bulk-create is fetched directly from GitHub (see package.json).
# npm defaults to blocking git dependencies entirely as a supply-chain
# safeguard; "root" trusts only git deps declared directly in this
# project's own package.json, not ones pulled in transitively.
allow-git=root
