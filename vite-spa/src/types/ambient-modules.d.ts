// Untyped third-party packages the original app also consumed without real
// types (next dev's SWC transpile never actually type-checked this - same
// story as mui.d.ts). zendro-bulk-create ships no types at all.
declare module 'zendro-bulk-create';
